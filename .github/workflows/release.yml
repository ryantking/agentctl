name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
        goos: [linux, darwin]
        goarch: [amd64, arm64]
        exclude:
          - os: ubuntu-latest
            goos: darwin
          - os: macos-latest
            goos: linux

    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          ref: ${{ github.event.repository.default_branch }}
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16 # v6
        with:
          go-version: '1.27.0'

      - name: Install Just
        uses: extractions/setup-just@f8a3cce218d9f83db3a2ecd90e41ac3de6cdfd9b # v3

      - name: Build binary
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: 0
        run: |
          VERSION=${{ github.ref_name }}
          COMMIT=$(git rev-parse --short HEAD)
          DATE=$(date -u +%Y-%m-%dT%H:%M:%SZ)
          LDFLAGS="-s -w -X github.com/ryantking/agentctl/cmd/agentctl.version=${VERSION} -X github.com/ryantking/agentctl/cmd/agentctl.commit=${COMMIT} -X github.com/ryantking/agentctl/cmd/agentctl.date=${DATE}"
          go build -ldflags "${LDFLAGS}" -trimpath -o agentctl ./cmd/agentctl

      - name: Upload artifacts
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
        with:
          name: agentctl-${{ matrix.goos }}-${{ matrix.goarch }}
          path: agentctl
          retention-days: 30

  release:
    needs: build-and-release
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Download all artifacts
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7
        with:
          pattern: agentctl-*
          merge-multiple: false

      - name: Create release archive
        run: |
          for dir in agentctl-*; do
            os_arch=$(echo $dir | sed 's/agentctl-//')
            tar -czf "agentctl-${GITHUB_REF_NAME}-${os_arch}.tar.gz" -C "$dir" agentctl
          done

      - name: Create checksums
        run: |
          sha256sum agentctl-*.tar.gz > checksums.txt

      - name: Create GitHub Release
        uses: softprops/action-gh-release@3bb12739c298aeb8a4eeaf626c5b8d85266b0e65 # v2
        with:
          files: |
            agentctl-*.tar.gz
            checksums.txt
          generate_release_notes: true
          body: |
            ## Installation

            ### Via Homebrew (macOS)
            ```bash
            brew install ryantking/tap/agentctl
            ```

            ### Download Binary
            Download the appropriate binary for your platform from the assets below.

            ### From source
            ```bash
            git clone https://github.com/ryantking/agentctl.git
            cd agentctl
            git checkout ${{ github.ref_name }}
            just install
            ```
